Module('RestAPI')({
  buildFromRestParams : function(params) {
    var queryBuilder = this.query();

    // .offset(req.query.offset || 0)
    // .limit(controller.constructor.INSTALLATIONS_PER_PAGE).then(function(results) {

    return queryBuilder;
  }
});
